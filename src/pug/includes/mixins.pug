mixin info(label, path, data = [])
  .info
    .info__icon
      img(src=path, alt="")
    .info__content
      h4.info__label= label
      ul.info__list
      each data_item in data 
        li 
          if data_item.link
            a(href="#")= data_item.text
          else if data_item.link_dark
            a.info--dark(href="#")= data_item.text
          else
            span= data_item.text